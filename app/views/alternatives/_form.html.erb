<%= form_with(model: alternative, local: true) do |form| %>
  <% if alternative.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(alternative.errors.count, "error") %> prohibited this alternative from being saved:</h2>

      <ul>
      <% alternative.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :statement %>
    <%= form.text_area :statement, id: :alternative_statement %>
  </div>

  <div class="field">
    <%= form.label :question_id %>
    <%= form.text_field :question_id, id: :alternative_question_id %>
  </div>

  <div class="field">
    <%= form.label :veracity %>
    <%= form.check_box :veracity, id: :alternative_veracity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
