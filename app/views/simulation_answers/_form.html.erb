<%= form_with(model: simulation_answer, local: true) do |form| %>
  <% if simulation_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(simulation_answer.errors.count, "error") %> prohibited this simulation_answer from being saved:</h2>

      <ul>
      <% simulation_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :simulation_answer_user_id %>
  </div>

  <div class="field">
    <%= form.label :simulation_id %>
    <%= form.text_field :simulation_id, id: :simulation_answer_simulation_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
